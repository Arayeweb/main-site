export default function ManagerBlogTagsPage() {
  return (
    <div dir="rtl" className="p-6 text-sm text-slate-600">
      مدیریت تگ‌ها — از API <code>/api/admin/blog/tags</code> در دسترس است.
    </div>
  );
}
